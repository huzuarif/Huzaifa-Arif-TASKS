var numbers = [1, 2, 3, 4, 5];
// Intentional error: Accessing an index that is out of bounds
console.log(numbers[10]); // Trying to access index 10 which doesn't exist
// Corrected version:
// console.log(numbers[2]); // Accessing a valid index
